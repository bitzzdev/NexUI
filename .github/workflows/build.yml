name: Build NexUI

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          # Minecraft 26.2 requires Java 25 — keep in sync with build.gradle's
          # sourceCompatibility/targetCompatibility and the `--release 25` flag.
          java-version: '25'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build NexUI Mod Jar
        run: ./gradlew build --no-daemon

      - name: Upload Built Mod Artifact
        uses: actions/upload-artifact@v4
        with:
          name: nexui-mod-jar
          path: build/libs/*.jar
          if-no-files-found: error
